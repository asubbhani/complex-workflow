name: complex

on: push

jobs:
  ubuntu:
   runs-on: ubuntu-latest
   steps:
   - run: date
  windows:
   runs-on: windwows-latest
   steps:
   - run: date
  macos:
   runs-on: macs-latest
   steps:
   - run: date
  depends:
   needs: [ubuntu, windows, macos]
   runs-on: macs-latest
   steps:
   - run: date
